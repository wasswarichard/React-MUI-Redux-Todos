export interface ITODO {
    userId: number,
    id: number,
    title: string,
    completed: boolean,
    status: string
}

export interface IDESCRIPTION {
    status: string,
    id: number
}
